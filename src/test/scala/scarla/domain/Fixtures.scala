// package scarla.domain

// object Fixtures {

  // def spec(nd: Int = 2, na: Int = 2): DomainSpec = new {
    // val N_DIMENSIONS = nd
    // val N_ACTIONS = na

    // val D_LIMITS = Vector.fill(nd)((0.0, 9.0))
  // } with DomainSpec
// }
